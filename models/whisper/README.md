# Whisper model

Place `ggml-small.bin` in this directory. The model is intentionally excluded
from Git because it is approximately 465 MB, but it must be included in release
artifacts and Docker build contexts.
